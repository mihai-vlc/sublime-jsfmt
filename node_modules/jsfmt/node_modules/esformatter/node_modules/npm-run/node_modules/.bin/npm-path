#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../npm-path/bin/npm-path" "$@"
  ret=$?
else 
  node  "$basedir/../npm-path/bin/npm-path" "$@"
  ret=$?
fi
exit $ret
